/* This generates a multi-colored gradient */

fn red(W : int, H : int) : rgba[,] {
        let red_image = array[i: H, j: W] \
                rgba {1.0, 0.0, 0.0, 1.0}
        return red_image
}

let W = 800
let H = 600
write image red(W, H) to "red.png"
